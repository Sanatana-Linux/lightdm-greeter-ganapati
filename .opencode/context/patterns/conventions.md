# Coding Conventions — LightDM Elephant Greeter Go Rewrite

> Auto-generated by /init-project setup --full (Phase 6). Re-run to update.

## Naming

- **Variables**: camelCase, short receiver/local names (e.g. `u`, `err`)
- **Functions**: PascalCase for exported, camelCase for unexported
- **Structs**: PascalCase
- **Files**: Lowercase single-word, or snake_case for tests
- **Constants**: PascalCase or SCREAMING_SNAKE_CASE
- **Components**: PascalCase for UI controls

## File Organization

- **Source directory**: `cmd/` for entrypoint, `pkg/` for reusable libraries
- **Test directory**: Co-located in `pkg/` with `_test.go` suffix
- **Barrel files**: None

## Error Handling

- **Style**: Explicit return with error wrapping (e.g. `fmt.Errorf("do task: %w", err)`)
- **Custom errors**: Declared at package level using custom struct implementing `error`

## Testing

- **Framework**: Standard library `testing` package
- **Location**: Co-located with implementation
- **Naming**: `TestXxx(t *testing.T)`
- **Mocking**: Mock structs satisfying interface contracts

## Imports

- **Style**: Grouped alphabetically with standard library first, followed by third-party packages.

## Code Style

- **Indent**: Tabs (standard `gofmt` style)
- **Quotes**: Double quotes for strings
- **Line length**: Standard line limits (avoid overly long lines, keep readable)

## Git Conventions

- **Commits**: Conventional Commits specification
- **Branch naming**: `feature/*` or `fix/*`
