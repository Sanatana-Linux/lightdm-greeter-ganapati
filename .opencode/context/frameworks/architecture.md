# Architecture — LightDM Elephant Greeter Go Rewrite

> Auto-generated by /init-project setup --full (Phase 6). Re-run to update.

## Module Structure

| Module | Path | Responsibility | Depends On | Depended By |
|--------|------|----------------|------------|-------------|
| `cmd/lightdm-elephant-greeter` | `cmd/lightdm-elephant-greeter` | Main application entrypoint and signal handling | `pkg/dbus`, `pkg/ui` | |
| `pkg/dbus` | `pkg/dbus` | LightDM D-Bus integration and state-machine wrapping | | `cmd/lightdm-elephant-greeter` |
| `pkg/ui` | `pkg/ui` | GPU-accelerated desktop session selector graphical UI (Gio) | | `cmd/lightdm-elephant-greeter` |

## Design Patterns

- **Functional Options Pattern**: Used in `pkg/ui` to customize layout options on UI components dynamically.
- **State Machine Pattern**: Used in `pkg/dbus` to wrap the discrete steps of LightDM authentication sequence in Go.

## Layering

- **cmd (Entrypoint)**: Orchestrates startup, sets up connection, boots window loop.
- **pkg/ui (Presentation)**: Pure Go GPU drawing layer rendering text fields, lists, and button controls.
- **pkg/dbus (Communications)**: Translates local authentication states to freedesktop display manager D-Bus IPC methods.

## Data Flow

User inputs credentials -> `pkg/ui` collects input -> `pkg/dbus` transmits over D-Bus -> LightDM replies with prompts -> `pkg/ui` displays prompts -> User responds -> authentication succeeds -> `pkg/dbus` calls `StartSession` with selected session ID.

## Key Abstractions

- `GreeterClient` (Interface, `pkg/dbus/client.go`): Abstracts LightDM display manager capability list.

## Coupling Analysis

The modules `pkg/ui` and `pkg/dbus` remain completely independent and decoupled, only meeting at the `cmd/` entrypoint where callbacks are passed to integrate UI interactions with authentication events.
